# OrchestraOS Architecture

Placeholder — gm authors the public architecture doc for C2 (the hackathon
checklist item). The private tree's ARCHITECTURE.md is the operator's live-ops
reference (real Tailscale IPs and SSH logins for other people's machines, the
literal production crontab, business-specific cron jobs) and was not safe to
carry over even as a "seed" — see EXTRACTION_REPORT.md section 7d.

What this doc should cover once written: seat/generation model, the gateway,
the approvals surface, msg_store, Arturo, the runtime catalog, blue-green
rotation. One diagram (per C2).
