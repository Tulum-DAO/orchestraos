---
name: RED ALERT (crash / bug / improvement / suggestion)
about: Same shape as the in-app report button — paste the report the system filed for you
labels: red-alert
---

<!-- In the app: agent page → siren button → File report. It writes
     $ORCHESTRA_DIR/state/red-alert/<ts>-<seat>-<class>.json and captures the evidence.
     Paste that JSON (or its id + the symptom) below. Nothing here reaches anyone's fleet but yours. -->

**Kind:** crash | bug | improvement | suggestion
**Seat:** (agent id, e.g. gm)
**What you saw (your words, verbatim):**

**Report id / JSON** (from `python3 scripts/red_alert.py list`, or the sheet's `ra_…` id):

```json
```

**OrchestraOS version / commit:** `git rev-parse --short HEAD`
**Runtime(s):** claude / gemini / codex
